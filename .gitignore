# Business data — contains prospect contact details and the ledger. Never commit.
data/
*.db
*.db-wal
*.db-shm

# Local configuration and secrets
answerrank.yml
# Personal financial data. (A comment on the same line as a pattern
# becomes part of the pattern, which is how this file went unignored.)
budget.yml
.env
.env.local
# Mailbox password and API keys.
keys.env
# Server setup files carry a copy of keys.env.
server-setup-*.sh
backups/
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
